/**
 * Camada de INFRAESTRUTURA — adapters de entrada (rest, scheduler, websocket) e
 * de saída (provider HTTP, redis, persistence), config e observabilidade.
 * Implementa as portas definidas em application/.
 */
package com.fajtech.sppotracker.infrastructure;
